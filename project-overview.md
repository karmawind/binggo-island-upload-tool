{
 "cells": [
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "# 灵感岛推文分发器 — 项目全景图\n",
    "\n",
    "> 一键多平台社交媒体内容发布工具\n",
    "\n",
    "项目由 **Python Flask 后端** + **Vue.js 前端** + **CLI 命令行** 三部分组成，支持 10 个主流社交媒体平台的视频/图文自动发布。"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "import matplotlib.pyplot as plt\n",
    "import matplotlib.patches as mpatches\n",
    "from matplotlib.patches import FancyBboxPatch, FancyArrowPatch\n",
    "import numpy as np\n",
    "\n",
    "plt.rcParams['font.sans-serif'] = ['SimHei', 'Microsoft YaHei', 'DejaVu Sans']\n",
    "plt.rcParams['axes.unicode_minus'] = False"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## 1. 整体架构"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "fig, ax = plt.subplots(figsize=(20, 12))\n",
    "ax.set_xlim(0, 20)\n",
    "ax.set_ylim(0, 12)\n",
    "ax.axis('off')\n",
    "ax.set_title('灵感岛推文分发器 — 整体架构图', fontsize=20, fontweight='bold', pad=20)\n",
    "\n",
    "# ===== 用户入口层 =====\n",
    "user_box = FancyBboxPatch((0.5, 9.5), 3.5, 2, boxstyle=\"round,pad=0.2\", facecolor='#E3F2FD', edgecolor='#1565C0', linewidth=2)\n",
    "ax.add_patch(user_box)\n",
    "ax.text(2.25, 10.9, '👤 用户入口', fontsize=14, fontweight='bold', ha='center', color='#1565C0')\n",
    "ax.text(2.25, 10.35, 'Vue.js 前端 (localhost:5173)', fontsize=10, ha='center')\n",
    "ax.text(2.25, 10.0, 'CLI 命令行 (sau ...)', fontsize=10, ha='center')\n",
    "\n",
    "# ===== 后端服务层 =====\n",
    "backend_box = FancyBboxPatch((5.5, 9.0), 4, 2.5, boxstyle=\"round,pad=0.2\", facecolor='#FFF3E0', edgecolor='#E65100', linewidth=2)\n",
    "ax.add_patch(backend_box)\n",
    "ax.text(7.5, 11.1, '⚙️ Flask 后端 (localhost:5409)', fontsize=13, fontweight='bold', ha='center', color='#E65100')\n",
    "ax.text(7.5, 10.5, 'RESTful API + SSE 实时通信', fontsize=10, ha='center')\n",
    "ax.text(7.5, 10.1, '文件上传 / 账号管理 / 发布调度', fontsize=10, ha='center')\n",
    "ax.text(7.5, 9.7, '多线程异步发布', fontsize=10, ha='center')\n",
    "\n",
    "# ===== 数据存储层 =====\n",
    "db_box = FancyBboxPatch((11, 9.5), 3.5, 2, boxstyle=\"round,pad=0.2\", facecolor='#E8F5E9', edgecolor='#2E7D32', linewidth=2)\n",
    "ax.add_patch(db_box)\n",
    "ax.text(12.75, 11.1, '💾 数据存储', fontsize=14, fontweight='bold', ha='center', color='#2E7D32')\n",
    "ax.text(12.75, 10.5, 'SQLite (database.db)', fontsize=10, ha='center')\n",
    "ax.text(12.75, 10.1, 'Cookie 文件 (cookies/)', fontsize=10, ha='center')\n",
    "ax.text(12.75, 9.7, '媒体文件 (videoFile/ imageFile/)', fontsize=10, ha='center')\n",
    "\n",
    "# ===== 浏览器自动化层 =====\n",
    "auto_box = FancyBboxPatch((5.5, 4.0), 4, 4.2, boxstyle=\"round,pad=0.2\", facecolor='#F3E5F5', edgecolor='#6A1B9A', linewidth=2)\n",
    "ax.add_patch(auto_box)\n",
    "ax.text(7.5, 7.8, '🤖 浏览器自动化引擎', fontsize=13, fontweight='bold', ha='center', color='#6A1B9A')\n",
    "ax.text(7.5, 7.2, 'Patchright (反检测 Chromium)', fontsize=10, ha='center')\n",
    "ax.text(7.5, 6.8, '', fontsize=10, ha='center')\n",
    "ax.text(7.5, 6.4, '┌─ 视频上传器 ─────────┐', fontsize=9, ha='center', family='monospace')\n",
    "ax.text(7.5, 6.0, '│ 抖音 · 快手 · 小红书   │', fontsize=9, ha='center', family='monospace')\n",
    "ax.text(7.5, 5.6, '│ B站 · 视频号 · TikTok  │', fontsize=9, ha='center', family='monospace')\n",
    "ax.text(7.5, 5.2, '├─ 图文上传器 ─────────┤', fontsize=9, ha='center', family='monospace')\n",
    "ax.text(7.5, 4.8, '│ 百家号 · 什么值得买    │', fontsize=9, ha='center', family='monospace')\n",
    "ax.text(7.5, 4.4, '│ 头条号 · 携程          │', fontsize=9, ha='center', family='monospace')\n",
    "ax.text(7.5, 4.0, '└───────────────────────┘', fontsize=9, ha='center', family='monospace')\n",
    "\n",
    "# ===== 目标平台层 =====\n",
    "platforms = [\n",
    "    ('抖音', '#FE2C55'), ('快手', '#FF4906'), ('小红书', '#FF2442'),\n",
    "    ('B站', '#00A1D6'), ('百家号', '#2932E1'), ('头条号', '#FF0000'),\n",
    "    ('什么值得买', '#E4393C'), ('携程', '#0086F6'), ('TikTok', '#000000'),\n",
    "    ('视频号', '#07C160')\n",
    "]\n",
    "for i, (name, color) in enumerate(platforms):\n",
    "    col = i % 5\n",
    "    row = i // 5\n",
    "    x = 0.5 + col * 1.7\n",
    "    y = 2.0 - row * 1.2\n",
    "    p_box = FancyBboxPatch((x, y), 1.5, 0.8, boxstyle=\"round,pad=0.1\", facecolor=color, edgecolor=color, linewidth=1.5, alpha=0.85)\n",
    "    ax.add_patch(p_box)\n",
    "    ax.text(x + 0.75, y + 0.4, name, fontsize=11, fontweight='bold', ha='center', va='center', color='white')\n",
    "\n",
    "ax.text(5.5, 1.8, '🌍 目标社交平台', fontsize=13, fontweight='bold', color='#333')\n",
    "\n",
    "# ===== 箭头连线 =====\n",
    "arrow_style = dict(arrowstyle='->', color='#666', linewidth=1.5, connectionstyle='arc3,rad=0')\n",
    "# 用户 → 后端\n",
    "ax.annotate('', xy=(5.5, 10.5), xytext=(4.0, 10.5), arrowprops=arrow_style)\n",
    "# 后端 → 存储\n",
    "ax.annotate('', xy=(11.0, 10.5), xytext=(9.5, 10.5), arrowprops=arrow_style)\n",
    "# 后端 → 自动化\n",
    "ax.annotate('', xy=(7.5, 8.2), xytext=(7.5, 9.0), arrowprops=arrow_style)\n",
    "# 自动化 → 平台\n",
    "ax.annotate('', xy=(5.0, 2.5), xytext=(6.0, 4.0), arrowprops=arrow_style)\n",
    "\n",
    "plt.tight_layout()\n",
    "plt.savefig('project_architecture.png', dpi=150, bbox_inches='tight', facecolor='white')\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## 2. 核心功能路径\n",
    "\n",
    "项目有 **两条核心功能路径**：视频发布 和 图文发布"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(22, 10))\n",
    "\n",
    "for ax in (ax1, ax2):\n",
    "    ax.set_xlim(0, 10)\n",
    "    ax.set_ylim(0, 12)\n",
    "    ax.axis('off')\n",
    "\n",
    "ax1.set_title('📹 视频发布路径', fontsize=16, fontweight='bold', color='#1565C0', pad=15)\n",
    "ax2.set_title('📝 图文发布路径', fontsize=16, fontweight='bold', color='#E65100', pad=15)\n",
    "\n",
    "# ===== 视频发布流程 =====\n",
    "video_steps = [\n",
    "    (5, 11.0, '① 用户上传视频', '前端 / CLI', '#E3F2FD', '#1565C0'),\n",
    "    (5, 9.5, '② 选择平台和账号', '勾选多个平台', '#E8F5E9', '#2E7D32'),\n",
    "    (5, 8.0, '③ 填写元数据', '标题/描述/标签/定时', '#FFF3E0', '#E65100'),\n",
    "    (5, 6.5, '④ 后端接收请求', 'POST /postVideo', '#FCE4EC', '#C62828'),\n",
    "    (5, 5.0, '⑤ Cookie 验证', '检查登录状态', '#F3E5F5', '#6A1B9A'),\n",
    "    (5, 3.5, '⑥ Patchright 自动化', '打开浏览器/填表/上传', '#E0F7FA', '#00695C'),\n",
    "    (5, 2.0, '⑦ 发布完成', '状态回写数据库', '#E8F5E9', '#2E7D32'),\n",
    "]\n",
    "\n",
    "for x, y, title, desc, fc, ec in video_steps:\n",
    "    box = FancyBboxPatch((x-3, y-0.5), 6, 1, boxstyle=\"round,pad=0.15\", facecolor=fc, edgecolor=ec, linewidth=1.5)\n",
    "    ax1.add_patch(box)\n",
    "    ax1.text(x-2.5, y+0.15, title, fontsize=11, fontweight='bold', va='center', color=ec)\n",
    "    ax1.text(x+2.5, y-0.15, desc, fontsize=9, ha='right', va='center', color='#666')\n",
    "\n",
    "# 箭头\n",
    "for i in range(len(video_steps)-1):\n",
    "    ax1.annotate('', xy=(5, video_steps[i+1][1]+0.5), xytext=(5, video_steps[i][1]-0.5),\n",
    "                 arrowprops=dict(arrowstyle='->', color='#999', linewidth=1.5))\n",
    "\n",
    "# 支持平台标注\n",
    "ax1.text(5, 0.8, '支持: 抖音 · 快手 · 小红书 · B站 · 视频号 · TikTok', fontsize=10, ha='center', color='#666', style='italic')\n",
    "\n",
    "# ===== 图文发布流程 =====\n",
    "article_steps = [\n",
    "    (5, 11.0, '① 编辑文章内容', '标题/正文/图片', '#E3F2FD', '#1565C0'),\n",
    "    (5, 9.5, '② 选择发布平台', '百家号/头条/值得买/携程', '#FFF3E0', '#E65100'),\n",
    "    (5, 8.0, '③ 选择账号', '每个平台各选账号', '#E8F5E9', '#2E7D32'),\n",
    "    (5, 6.5, '④ 后端接收请求', 'POST /postArticle', '#FCE4EC', '#C62828'),\n",
    "    (5, 5.0, '⑤ 分发到各平台', '多线程并行发布', '#F3E5F5', '#6A1B9A'),\n",
    "    (5, 3.5, '⑥ 浏览器自动化操作', '编辑器填入/图片上传/点击发布', '#E0F7FA', '#00695C'),\n",
    "    (5, 2.0, '⑦ 结果汇总', '成功/失败 + 审核', '#E8F5E9', '#2E7D32'),\n",
    "]\n",
    "\n",
    "for x, y, title, desc, fc, ec in article_steps:\n",
    "    box = FancyBboxPatch((x-3, y-0.5), 6, 1, boxstyle=\"round,pad=0.15\", facecolor=fc, edgecolor=ec, linewidth=1.5)\n",
    "    ax2.add_patch(box)\n",
    "    ax2.text(x-2.5, y+0.15, title, fontsize=11, fontweight='bold', va='center', color=ec)\n",
    "    ax2.text(x+2.5, y-0.15, desc, fontsize=9, ha='right', va='center', color='#666')\n",
    "\n",
    "for i in range(len(article_steps)-1):\n",
    "    ax2.annotate('', xy=(5, article_steps[i+1][1]+0.5), xytext=(5, article_steps[i][1]-0.5),\n",
    "                 arrowprops=dict(arrowstyle='->', color='#999', linewidth=1.5))\n",
    "\n",
    "ax2.text(5, 0.8, '编辑器: UEditor(百家号) · ProseMirror(头条/值得买) · Draft.js(携程)', fontsize=9, ha='center', color='#666', style='italic')\n",
    "\n",
    "plt.tight_layout()\n",
    "plt.savefig('functional_paths.png', dpi=150, bbox_inches='tight', facecolor='white')\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## 3. 目录结构与文件职责"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "fig, ax = plt.subplots(figsize=(18, 14))\n",
    "ax.set_xlim(0, 18)\n",
    "ax.set_ylim(0, 14)\n",
    "ax.axis('off')\n",
    "ax.set_title('📁 项目目录结构与文件职责', fontsize=18, fontweight='bold', pad=20)\n",
    "\n",
    "# 根目录文件\n",
    "root_items = [\n",
    "    ('sau_backend.py', 'Flask 后端主程序', '#FFF3E0'),\n",
    "    ('sau_cli.py', 'CLI 命令行入口', '#E3F2FD'),\n",
    "    ('conf.py', '项目配置 (BASE_DIR等)', '#F5F5F5'),\n",
    "    ('requirements.txt', 'Python 依赖清单', '#F5F5F5'),\n",
    "]\n",
    "\n",
    "ax.text(2, 13.2, '📂 根目录文件', fontsize=13, fontweight='bold', color='#333')\n",
    "for i, (name, desc, color) in enumerate(root_items):\n",
    "    y = 12.5 - i * 0.55\n",
    "    box = FancyBboxPatch((0.3, y-0.2), 7.5, 0.45, boxstyle=\"round,pad=0.05\", facecolor=color, edgecolor='#ccc', linewidth=0.8)\n",
    "    ax.add_patch(box)\n",
    "    ax.text(0.5, y, name, fontsize=9, fontweight='bold', va='center', family='monospace', color='#333')\n",
    "    ax.text(3.5, y, desc, fontsize=8, va='center', color='#666')\n",
    "\n",
    "# uploader 目录\n",
    "uploader_items = [\n",
    "    ('douyin_uploader/', '抖音上传器 (视频+图文笔记)', '#FE2C55'),\n",
    "    ('ks_uploader/', '快手上传器 (视频+图文笔记)', '#FF4906'),\n",
    "    ('xiaohongshu_uploader/', '小红书上传器 (视频+图文笔记)', '#FF2442'),\n",
    "    ('bilibili_uploader/', 'B站上传器 (视频)', '#00A1D6'),\n",
    "    ('tencent_uploader/', '视频号上传器 (视频)', '#07C160'),\n",
    "    ('tk_uploader/', 'TikTok 上传器 (视频)', '#000000'),\n",
    "    ('baijiahao_uploader/', '百家号上传器 (视频+图文)', '#2932E1'),\n",
    "    ('smzdm_uploader/', '什么值得买上传器 (图文)', '#E4393C'),\n",
    "    ('toutiao_uploader/', '头条号上传器 (图文)', '#FF0000'),\n",
    "    ('ctrip_uploader/', '携程上传器 (图文)', '#0086F6'),\n",
    "]\n",
    "\n",
    "ax.text(9.5, 13.2, '📂 uploader/ — 平台上传器', fontsize=13, fontweight='bold', color='#333')\n",
    "for i, (name, desc, color) in enumerate(uploader_items):\n",
    "    y = 12.5 - i * 0.55\n",
    "    box = FancyBboxPatch((9.2, y-0.2), 8.3, 0.45, boxstyle=\"round,pad=0.05\", facecolor='white', edgecolor=color, linewidth=1.5)\n",
    "    ax.add_patch(box)\n",
    "    ax.text(9.4, y, name, fontsize=9, fontweight='bold', va='center', family='monospace', color=color)\n",
    "    ax.text(14, y, desc, fontsize=8, va='center', color='#666')\n",
    "\n",
    "# 前端目录\n",
    "frontend_items = [\n",
    "    ('Dashboard.vue', '主控面板'),\n",
    "    ('AccountManagement.vue', '账号管理 (登录/Cookie)'),\n",
    "    ('MaterialManagement.vue', '素材管理 (视频/图片)'),\n",
    "    ('PublishCenter.vue', '视频发布中心'),\n",
    "    ('ArticlePublish.vue', '图文发布界面'),\n",
    "    ('ArticleManagement.vue', '文章管理列表'),\n",
    "]\n",
    "\n",
    "ax.text(2, 6.5, '📂 sau_frontend/src/views/', fontsize=13, fontweight='bold', color='#333')\n",
    "for i, (name, desc) in enumerate(frontend_items):\n",
    "    y = 5.9 - i * 0.55\n",
    "    box = FancyBboxPatch((0.3, y-0.2), 7.5, 0.45, boxstyle=\"round,pad=0.05\", facecolor='#E8F5E9', edgecolor='#4CAF50', linewidth=0.8)\n",
    "    ax.add_patch(box)\n",
    "    ax.text(0.5, y, name, fontsize=9, fontweight='bold', va='center', family='monospace', color='#2E7D32')\n",
    "    ax.text(3.5, y, desc, fontsize=8, va='center', color='#666')\n",
    "\n",
    "# myUtils 目录\n",
    "utils_items = [\n",
    "    ('auth.py', 'Cookie 验证 (各平台)'),\n",
    "    ('login.py', '登录工具函数'),\n",
    "    ('postVideo.py', '视频发布调度器'),\n",
    "    ('postArticle.py', '图文发布调度器 (多平台分发)'),\n",
    "]\n",
    "\n",
    "ax.text(9.5, 6.5, '📂 myUtils/ — 工具模块', fontsize=13, fontweight='bold', color='#333')\n",
    "for i, (name, desc) in enumerate(utils_items):\n",
    "    y = 5.9 - i * 0.55\n",
    "    box = FancyBboxPatch((9.2, y-0.2), 8.3, 0.45, boxstyle=\"round,pad=0.05\", facecolor='#F3E5F5', edgecolor='#9C27B0', linewidth=0.8)\n",
    "    ax.add_patch(box)\n",
    "    ax.text(9.4, y, name, fontsize=9, fontweight='bold', va='center', family='monospace', color='#6A1B9A')\n",
    "    ax.text(12.5, y, desc, fontsize=8, va='center', color='#666')\n",
    "\n",
    "# 数据库\n",
    "db_items = [\n",
    "    ('user_info', '账号信息 (id, type, filePath, userName)'),\n",
    "    ('file_records', '文件记录 (视频/图片路径)'),\n",
    "    ('article_posts', '文章草稿和已发布文章'),\n",
    "    ('article_images', '文章图片记录'),\n",
    "]\n",
    "\n",
    "ax.text(2, 3.2, '💾 数据库表 (SQLite)', fontsize=13, fontweight='bold', color='#333')\n",
    "for i, (name, desc) in enumerate(db_items):\n",
    "    y = 2.6 - i * 0.55\n",
    "    box = FancyBboxPatch((0.3, y-0.2), 7.5, 0.45, boxstyle=\"round,pad=0.05\", facecolor='#E0F7FA', edgecolor='#0097A7', linewidth=0.8)\n",
    "    ax.add_patch(box)\n",
    "    ax.text(0.5, y, name, fontsize=9, fontweight='bold', va='center', family='monospace', color='#00695C')\n",
    "    ax.text(3, y, desc, fontsize=8, va='center', color='#666')\n",
    "\n",
    "# 其他目录\n",
    "ax.text(9.5, 3.2, '📂 其他目录', fontsize=13, fontweight='bold', color='#333')\n",
    "other_items = [\n",
    "    ('cookies/', 'Cookie 存储目录 (JSON)'),\n",
    "    ('skills/', '技能包 (Agent Skills)'),\n",
    "    ('db/', '数据库文件和建表脚本'),\n",
    "]\n",
    "for i, (name, desc) in enumerate(other_items):\n",
    "    y = 2.6 - i * 0.55\n",
    "    box = FancyBboxPatch((9.2, y-0.2), 8.3, 0.45, boxstyle=\"round,pad=0.05\", facecolor='#FFF8E1', edgecolor='#F9A825', linewidth=0.8)\n",
    "    ax.add_patch(box)\n",
    "    ax.text(9.4, y, name, fontsize=9, fontweight='bold', va='center', family='monospace', color='#F57F17')\n",
    "    ax.text(12, y, desc, fontsize=8, va='center', color='#666')\n",
    "\n",
    "plt.tight_layout()\n",
    "plt.savefig('directory_structure.png', dpi=150, bbox_inches='tight', facecolor='white')\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## 4. 浏览器自动化核心机制\n",
    "\n",
    "项目使用 **Patchright**（Playwright 的反检测分支）驱动 Chromium 浏览器，模拟人工操作完成发布。"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "fig, ax = plt.subplots(figsize=(18, 10))\n",
    "ax.set_xlim(0, 18)\n",
    "ax.set_ylim(0, 10)\n",
    "ax.axis('off')\n",
    "ax.set_title('🤖 浏览器自动化核心机制', fontsize=18, fontweight='bold', pad=20)\n",
    "\n",
    "# 左侧：通用流程\n",
    "ax.text(4.5, 9.3, '通用自动化流程', fontsize=14, fontweight='bold', color='#6A1B9A')\n",
    "\n",
    "flow = [\n",
    "    'Cookie 加载 → 恢复登录态',\n",
    "    '导航到编辑器页面',\n",
    "    '清空编辑器残留状态',\n",
    "    '填写标题 / 正文内容',\n",
    "    '上传图片并插入正文',\n",
    "    '设置封面 / 标签 / 定时',\n",
    "    '点击发布按钮',\n",
    "    '验证发布成功',\n",
    "]\n",
    "\n",
    "for i, text in enumerate(flow):\n",
    "    y = 8.3 - i * 0.85\n",
    "    color = plt.cm.RdYlGn(0.2 + i * 0.1)\n",
    "    box = FancyBboxPatch((0.5, y-0.3), 8.5, 0.6, boxstyle=\"round,pad=0.1\", facecolor=color, edgecolor='#999', linewidth=1, alpha=0.3)\n",
    "    ax.add_patch(box)\n",
    "    ax.text(1, y, f'Step {i+1}', fontsize=9, fontweight='bold', va='center', color='#555')\n",
    "    ax.text(2.5, y, text, fontsize=10, va='center', color='#333')\n",
    "\n",
    "# 右侧：各平台编辑器差异\n",
    "ax.text(13.5, 9.3, '各平台编辑器差异', fontsize=14, fontweight='bold', color='#E65100')\n",
    "\n",
    "editors = [\n",
    "    ('百家号', 'UEditor (iframe)', 'execCommand(\"insertHTML\")', '#2932E1'),\n",
    "    ('头条号', 'ProseMirror', 'execCommand(\"insertHTML\")', '#FF0000'),\n",
    "    ('什么值得买', 'ProseMirror', 'execCommand(\"insertHTML\")', '#E4393C'),\n",
    "    ('携程', 'Draft.js (React)', 'keyboard.insert_text() 一次性输入', '#0086F6'),\n",
    "]\n",
    "\n",
    "for i, (platform, editor, method, color) in enumerate(editors):\n",
    "    y = 8.0 - i * 1.6\n",
    "    # 平台名\n",
    "    pbox = FancyBboxPatch((9.5, y+0.3), 2.5, 0.6, boxstyle=\"round,pad=0.1\", facecolor=color, edgecolor=color, linewidth=1.5, alpha=0.85)\n",
    "    ax.add_patch(pbox)\n",
    "    ax.text(10.75, y+0.6, platform, fontsize=11, fontweight='bold', ha='center', va='center', color='white')\n",
    "    # 编辑器类型\n",
    "    ebox = FancyBboxPatch((12.3, y+0.3), 2.5, 0.6, boxstyle=\"round,pad=0.1\", facecolor='#F5F5F5', edgecolor='#999', linewidth=1)\n",
    "    ax.add_patch(ebox)\n",
    "    ax.text(13.55, y+0.6, editor, fontsize=9, ha='center', va='center', color='#333')\n",
    "    # 填入方法\n",
    "    mbox = FancyBboxPatch((9.5, y-0.5), 5.3, 0.6, boxstyle=\"round,pad=0.1\", facecolor='#FFF8E1', edgecolor='#F9A825', linewidth=1)\n",
    "    ax.add_patch(mbox)\n",
    "    ax.text(9.7, y-0.2, '方法:', fontsize=9, va='center', fontweight='bold', color='#F57F17')\n",
    "    ax.text(10.8, y-0.2, method, fontsize=9, va='center', color='#333', family='monospace')\n",
    "\n",
    "# 关键技术标注\n",
    "ax.text(9, 1.5, '⚠️ 关键技术点', fontsize=12, fontweight='bold', color='#C62828')\n",
    "notes = [\n",
    "    '• Cookie 注入保持登录态 (免重复登录)',\n",
    "    '• dispatchEvent 绕过遮罩层拦截 (百家号/值得买)',\n",
    "    '• iframe focus 确保文件上传对话框触发',\n",
    "    '• 每次发布前 clear_editor() 清空残留状态',\n",
    "]\n",
    "for i, note in enumerate(notes):\n",
    "    ax.text(9, 0.9 - i * 0.4, note, fontsize=9, color='#555')\n",
    "\n",
    "plt.tight_layout()\n",
    "plt.savefig('automation_mechanism.png', dpi=150, bbox_inches='tight', facecolor='white')\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## 5. API 路由全览"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "fig, ax = plt.subplots(figsize=(16, 12))\n",
    "ax.set_xlim(0, 16)\n",
    "ax.set_ylim(0, 12)\n",
    "ax.axis('off')\n",
    "ax.set_title('🔌 Flask 后端 API 路由全览', fontsize=18, fontweight='bold', pad=20)\n",
    "\n",
    "api_groups = [\n",
    "    ('文件管理', '#E3F2FD', '#1565C0', [\n",
    "        ('POST /upload', '上传视频文件'),\n",
    "        ('POST /uploadSave', '上传并保存元数据'),\n",
    "        ('GET /getFiles', '获取所有文件列表'),\n",
    "        ('GET /deleteFile', '删除文件'),\n",
    "    ]),\n",
    "    ('账号管理', '#E8F5E9', '#2E7D32', [\n",
    "        ('GET /getAccounts', '获取所有账号'),\n",
    "        ('GET /getValidAccounts', '获取有效账号(含验证)'),\n",
    "        ('POST /uploadCookie', '上传 Cookie'),\n",
    "        ('GET /login', 'SSE 登录流程'),\n",
    "    ]),\n",
    "    ('视频发布', '#FFF3E0', '#E65100', [\n",
    "        ('POST /postVideo', '发布视频到单平台'),\n",
    "        ('POST /postVideoBatch', '批量发布视频'),\n",
    "    ]),\n",
    "    ('图文发布', '#FCE4EC', '#C62828', [\n",
    "        ('POST /postArticle', '发布文章(多平台)'),\n",
    "        ('GET /articleTaskStatus', '查询发布状态'),\n",
    "        ('POST /saveArticlePost', '保存文章草稿'),\n",
    "        ('POST /scheduleArticles', '定时发布文章'),\n",
    "        ('POST /importArticles', 'CSV 批量导入'),\n",
    "    ]),\n",
    "    ('图片管理', '#F3E5F5', '#6A1B9A', [\n",
    "        ('POST /uploadImage', '上传图片'),\n",
    "        ('GET /getImages', '获取图片列表'),\n",
    "        ('POST /copyMaterialToImage', '素材库复制'),\n",
    "    ]),\n",
    "]\n",
    "\n",
    "y_pos = 11.2\n",
    "for group_name, fc, ec, apis in api_groups:\n",
    "    # 组标题\n",
    "    header = FancyBboxPatch((0.5, y_pos - 0.3), 15, 0.55, boxstyle=\"round,pad=0.1\", facecolor=ec, edgecolor=ec, linewidth=1.5, alpha=0.15)\n",
    "    ax.add_patch(header)\n",
    "    ax.text(1, y_pos, group_name, fontsize=12, fontweight='bold', va='center', color=ec)\n",
    "    y_pos -= 0.7\n",
    "    \n",
    "    for route, desc in apis:\n",
    "        box = FancyBboxPatch((1, y_pos - 0.2), 14, 0.4, boxstyle=\"round,pad=0.05\", facecolor=fc, edgecolor=ec, linewidth=0.8)\n",
    "        ax.add_patch(box)\n",
    "        ax.text(1.3, y_pos, route, fontsize=9, fontweight='bold', va='center', family='monospace', color=ec)\n",
    "        ax.text(7, y_pos, desc, fontsize=9, va='center', color='#555')\n",
    "        y_pos -= 0.5\n",
    "    y_pos -= 0.3\n",
    "\n",
    "plt.tight_layout()\n",
    "plt.savefig('api_routes.png', dpi=150, bbox_inches='tight', facecolor='white')\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## 6. 前端页面流转"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "fig, ax = plt.subplots(figsize=(16, 8))\n",
    "ax.set_xlim(0, 16)\n",
    "ax.set_ylim(0, 8)\n",
    "ax.axis('off')\n",
    "ax.set_title('🖥️ 前端页面流转图', fontsize=18, fontweight='bold', pad=20)\n",
    "\n",
    "pages = [\n",
    "    (2, 5, 'Dashboard', '主控面板\\n概览统计', '#E3F2FD', '#1565C0'),\n",
    "    (6, 6.5, 'AccountManagement', '账号管理\\n扫码登录·Cookie管理', '#E8F5E9', '#2E7D32'),\n",
    "    (10, 6.5, 'MaterialManagement', '素材管理\\n视频·图片上传', '#FFF3E0', '#E65100'),\n",
    "    (6, 3, 'PublishCenter', '视频发布\\n选平台·选账号·填元数据', '#FCE4EC', '#C62828'),\n",
    "    (10, 3, 'ArticlePublish', '图文发布\\n编辑·多平台分发', '#F3E5F5', '#6A1B9A'),\n",
    "    (14, 3, 'ArticleManagement', '文章管理\\n草稿·已发布·定时', '#E0F7FA', '#00695C'),\n",
    "]\n",
    "\n",
    "for x, y, name, desc, fc, ec in pages:\n",
    "    box = FancyBboxPatch((x-1.5, y-1), 3, 2, boxstyle=\"round,pad=0.15\", facecolor=fc, edgecolor=ec, linewidth=2)\n",
    "    ax.add_patch(box)\n",
    "    ax.text(x, y+0.45, name, fontsize=10, fontweight='bold', ha='center', va='center', color=ec)\n",
    "    ax.text(x, y-0.2, desc, fontsize=8, ha='center', va='center', color='#555', linespacing=1.4)\n",
    "\n",
    "# 箭头连接\n",
    "arrows = [\n",
    "    ((3.5, 5.5), (4.5, 6.0)),   # Dashboard → AccountManagement\n",
    "    ((3.5, 4.5), (4.5, 3.5)),   # Dashboard → PublishCenter\n",
    "    ((7.5, 6.5), (8.5, 6.5)),   # AccountManagement → MaterialManagement\n",
    "    ((7.5, 3.0), (8.5, 3.0)),   # PublishCenter → ArticlePublish\n",
    "    ((11.5, 3.0), (12.5, 3.0)), # ArticlePublish → ArticleManagement\n",
    "    ((10, 5.5), (10, 4.0)),     # MaterialManagement → ArticlePublish\n",
    "]\n",
    "for start, end in arrows:\n",
    "    ax.annotate('', xy=end, xytext=start,\n",
    "                arrowprops=dict(arrowstyle='->', color='#999', linewidth=1.5))\n",
    "\n",
    "plt.tight_layout()\n",
    "plt.savefig('frontend_flow.png', dpi=150, bbox_inches='tight', facecolor='white')\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## 7. 多平台图文发布分发机制"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "fig, ax = plt.subplots(figsize=(16, 10))\n",
    "ax.set_xlim(0, 16)\n",
    "ax.set_ylim(0, 10)\n",
    "ax.axis('off')\n",
    "ax.set_title('🔀 多平台图文发布分发机制', fontsize=18, fontweight='bold', pad=20)\n",
    "\n",
    "# 用户提交\n",
    "user_box = FancyBboxPatch((5.5, 8.5), 5, 1, boxstyle=\"round,pad=0.15\", facecolor='#E3F2FD', edgecolor='#1565C0', linewidth=2)\n",
    "ax.add_patch(user_box)\n",
    "ax.text(8, 9.2, '用户提交文章 + 选择多平台', fontsize=12, fontweight='bold', ha='center', color='#1565C0')\n",
    "ax.text(8, 8.8, 'POST /postArticle', fontsize=10, ha='center', family='monospace', color='#666')\n",
    "\n",
    "# 后端分发器\n",
    "dispatch_box = FancyBboxPatch((5, 6.5), 6, 1.2, boxstyle=\"round,pad=0.15\", facecolor='#FFF3E0', edgecolor='#E65100', linewidth=2)\n",
    "ax.add_patch(dispatch_box)\n",
    "ax.text(8, 7.3, 'myUtils/postArticle.py', fontsize=11, fontweight='bold', ha='center', color='#E65100')\n",
    "ax.text(8, 6.9, 'dispatch_multi_platform() 多线程分发', fontsize=9, ha='center', color='#555')\n",
    "\n",
    "ax.annotate('', xy=(8, 7.7), xytext=(8, 8.5), arrowprops=dict(arrowstyle='->', color='#999', linewidth=2))\n",
    "\n",
    "# 各平台上传器\n",
    "platform_dispatch = [\n",
    "    ('百家号', 'post_article_baijiahao()', '#2932E1', 'UEditor + execCommand'),\n",
    "    ('头条号', 'post_article_toutiao()', '#FF0000', 'ProseMirror + execCommand'),\n",
    "    ('什么值得买', 'post_article_smzdm()', '#E4393C', 'ProseMirror + dispatchEvent'),\n",
    "    ('携程', 'post_article_ctrip()', '#0086F6', 'Draft.js + keyboard.type'),\n",
    "]\n",
    "\n",
    "for i, (name, func, color, method) in enumerate(platform_dispatch):\n",
    "    x = 1 + i * 3.8\n",
    "    # 线程\n",
    "    thread_box = FancyBboxPatch((x-0.5, 4.5), 3.3, 1.2, boxstyle=\"round,pad=0.1\", facecolor=color, edgecolor=color, linewidth=1.5, alpha=0.1)\n",
    "    ax.add_patch(thread_box)\n",
    "    ax.text(x+1.15, 5.4, name, fontsize=11, fontweight='bold', ha='center', color=color)\n",
    "    ax.text(x+1.15, 5.0, func, fontsize=7, ha='center', family='monospace', color='#555')\n",
    "    ax.text(x+1.15, 4.7, method, fontsize=7, ha='center', color='#888')\n",
    "    # 箭头从分发器到各线程\n",
    "    ax.annotate('', xy=(x+1.15, 5.7), xytext=(8, 6.5),\n",
    "                arrowprops=dict(arrowstyle='->', color=color, linewidth=1.5, alpha=0.6))\n",
    "\n",
    "# 各自操作浏览器\n",
    "for i, (name, func, color, method) in enumerate(platform_dispatch):\n",
    "    x = 1 + i * 3.8\n",
    "    browser_box = FancyBboxPatch((x-0.5, 2.5), 3.3, 1.2, boxstyle=\"round,pad=0.1\", facecolor='#F5F5F5', edgecolor='#999', linewidth=1)\n",
    "    ax.add_patch(browser_box)\n",
    "    ax.text(x+1.15, 3.4, '🤖 Patchright', fontsize=9, ha='center', color='#555')\n",
    "    ax.text(x+1.15, 3.0, '浏览器自动化', fontsize=8, ha='center', color='#888')\n",
    "    ax.annotate('', xy=(x+1.15, 3.7), xytext=(x+1.15, 4.5),\n",
    "                arrowprops=dict(arrowstyle='->', color='#999', linewidth=1))\n",
    "\n",
    "# 结果汇总\n",
    "result_box = FancyBboxPatch((4, 0.5), 8, 1, boxstyle=\"round,pad=0.15\", facecolor='#E8F5E9', edgecolor='#2E7D32', linewidth=2)\n",
    "ax.add_patch(result_box)\n",
    "ax.text(8, 1.2, '结果汇总 → 写入数据库 → 前端展示', fontsize=11, fontweight='bold', ha='center', color='#2E7D32')\n",
    "ax.text(8, 0.8, 'article_posts.publish_result (JSON)', fontsize=9, ha='center', family='monospace', color='#555')\n",
    "\n",
    "for i in range(4):\n",
    "    x = 1 + i * 3.8 + 1.15\n",
    "    ax.annotate('', xy=(8, 1.5), xytext=(x, 2.5),\n",
    "                arrowprops=dict(arrowstyle='->', color='#999', linewidth=1, alpha=0.5))\n",
    "\n",
    "plt.tight_layout()\n",
    "plt.savefig('multi_platform_dispatch.png', dpi=150, bbox_inches='tight', facecolor='white')\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## 8. 总结"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "fig, ax = plt.subplots(figsize=(14, 6))\n",
    "ax.set_xlim(0, 14)\n",
    "ax.set_ylim(0, 6)\n",
    "ax.axis('off')\n",
    "ax.set_title('📋 项目功能总结', fontsize=18, fontweight='bold', pad=15)\n",
    "\n",
    "summary = [\n",
    "    ('🎯 核心价值', '一键将视频/图文内容发布到 10 个社交媒体平台（含 TikTok、视频号）', '#E3F2FD', '#1565C0'),\n",
    "    ('📹 视频发布', '抖音·快手·小红书·B站·视频号·TikTok — 6个平台', '#FFF3E0', '#E65100'),\n",
    "    ('📝 图文发布', '百家号·头条号·什么值得买·携程 — 4个平台', '#FCE4EC', '#C62828'),\n",
    "    ('🤖 自动化引擎', 'Patchright (反检测Chromium) + 各平台定制化操作 SOP', '#F3E5F5', '#6A1B9A'),\n",
    "    ('🖥️ 三种入口', 'Web前端(Vue) + CLI命令行 + Agent技能包', '#E8F5E9', '#2E7D32'),\n",
    "    ('🔀 多平台分发', '一次编写 → 多线程并行发布到选中的所有平台', '#E0F7FA', '#00695C'),\n",
    "]\n",
    "\n",
    "for i, (icon_title, desc, fc, ec) in enumerate(summary):\n",
    "    y = 5.0 - i * 0.8\n",
    "    box = FancyBboxPatch((0.5, y-0.3), 13, 0.6, boxstyle=\"round,pad=0.1\", facecolor=fc, edgecolor=ec, linewidth=1.5)\n",
    "    ax.add_patch(box)\n",
    "    ax.text(1, y, icon_title, fontsize=11, fontweight='bold', va='center', color=ec)\n",
    "    ax.text(5, y, desc, fontsize=10, va='center', color='#333')\n",
    "\n",
    "plt.tight_layout()\n",
    "plt.savefig('summary.png', dpi=150, bbox_inches='tight', facecolor='white')\n",
    "plt.show()"
   ]
  }
 ],
 "metadata": {
  "kernelspec": {
   "display_name": "Python 3",
   "language": "python",
   "name": "python3"
  },
  "language_info": {
   "name": "python",
   "version": "3.11.0"
  }
 },
 "nbformat": 4,
 "nbformat_minor": 4
}
